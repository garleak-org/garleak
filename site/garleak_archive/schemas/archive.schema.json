{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://garleak.org/schema/archive/archive.yaml/0.1",
  "title": "archive.yaml: settings for one archive directory",
  "type": "object",
  "additionalProperties": false,
  "required": ["name"],
  "properties": {
    "name": {"type": "string", "minLength": 1},
    "example": {"type": "boolean", "description": "true only for archive-example; the site builds it under /example/ with noindex"},
    "as_of": {"type": ["string", "null"], "format": "date", "description": "Fixed 'today' for listings. Leave null for the real archive, which uses the build date."},
    "recent_count": {"type": "integer", "minimum": 1, "maximum": 500, "description": "How many entries the recent listing shows"},
    "config_version": {"type": "string"},
    "spec_version": {"type": "string"},
    "rubrics": {"type": "string", "description": "Path to packages/rubrics, relative to this file"}
  }
}
