# SPDX-License-Identifier: AGPL-3.0-or-later
"""Static site generator for Garleak (garleak.org).

    build     the site from archive/ (at the root) and archive-example/ (under /example/)
    view      view models over garleak_archive
    markup    Markdown rendering, safe for record bodies
    pdfstamp  the notice stamped on every page of every PDF (SPEC §9.3)
    checks    link, markup, wording and indexing checks over a built site
"""

__version__ = "0.1.0"
